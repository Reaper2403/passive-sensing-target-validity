# Local data directory

No data files are tracked here. Authorized GLOBEM users should place version 1.1 at
`data/raw/globem/1.1/` and follow the instructions in the repository-level
`DATA_ACCESS.md`.

Raw, interim, processed, and external data directories are ignored by Git.
